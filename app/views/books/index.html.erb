




<body>
	<div class="container">
		<div class="row">
			<% if @book.errors.any? %>
				<div class="error_field">
					<h2 style="color: red;"><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
					<ul>
						<% @book.errors.full_messages.each do |message| %>
							<li style="color: red;"><%= message %></li>
						<% end %>
					</ul>
				</div>
			<% end %>
		</div>
		<div class="row">
			<%= render 'users/user-info', book: @book , user: @user %>
			<div class="col-xs-9">
				<div class="books_index">
					<h2>Books</h2>
					<table class="table table-striped">
						<thead>
							<th></th>
							<th>Title</th>
							<th>Opinion</th>
							<th></th>
						</thead>
						<% @books.each do |book| %>
						<tbody>
							<tr>
								<td><%= link_to attachment_image_tag(book.user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "40x40"), user_path(book.user) %></td>
								<td><%= link_to book.title, book_path(book.id) %></td>
								<td><%= book.body %></td>
							</tr>
						</tbody>
						<% end %>
					</table>
				</div>
			</div>
		</div>
	</div>
	
</body>